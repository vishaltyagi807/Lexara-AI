"""database — core-layer database access (placeholder).

Note: the API layer (backend/api/app/db/) already manages database sessions
and SQLAlchemy models for the auth/user domain. This package is reserved for
any data-persistence needs of the AI pipeline itself (e.g. conversation
history, audit logs, cost tracking).
"""
