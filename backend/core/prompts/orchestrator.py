"""orchestrator.py — system prompt(s) for the orchestrator agent.

Currently a placeholder. The orchestrator performs input validation only
and does not call an LLM; no prompt is needed today.
Add prompt constants here when the orchestrator gains LLM capabilities.
"""

# Example future constant:
# ORCHESTRATOR_SYSTEM_PROMPT: str = "..."
