"""router.py — system prompt(s) for the router agent.

Currently a placeholder. The router selects downstream agents via registry
lookup and does not call an LLM; no prompt is needed today.
Add prompt constants here when the router gains LLM-based routing capabilities
(e.g. semantic routing, policy-aware routing).
"""

# Example future constant:
# ROUTER_SYSTEM_PROMPT: str = "..."
