"""providers — LLM and external API provider abstractions (placeholder).

Future: add provider wrappers here for OpenAI, Anthropic, Groq, etc.
Each provider should expose a consistent async interface so the service
layer is decoupled from any specific SDK.
"""
