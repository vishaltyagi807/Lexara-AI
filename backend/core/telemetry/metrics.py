"""metrics.py — application metrics for the Lexara AI core (placeholder).

Future: integrate Prometheus / OpenTelemetry metrics here.

Example future constants / helpers:
    from prometheus_client import Counter, Histogram
    GRAPH_INVOCATIONS = Counter("lexara_graph_invocations_total", ...)
    ...
"""

# Placeholder — no custom metrics configured yet.
# Prometheus scrape endpoint is provided by prometheus-fastapi-instrumentator
# in the FastAPI layer (api/app/main.py).
